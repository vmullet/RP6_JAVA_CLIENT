package enums;

/**
 * Enum to manage the diferent type of trajectory possible (executed once or in an infinite loop)
 * @author Valentin
 *
 */
public enum TrajectoryMode {

	NONE, 
	ONCE, 
	LOOP

}
